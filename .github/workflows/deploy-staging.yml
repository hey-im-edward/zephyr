name: Deploy Staging

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - "backend/**"
      - ".github/workflows/deploy-staging.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-staging-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    services:
      mysql:
        image: mysql:8.1
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: shoe_store
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -proot --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=12
    environment:
      name: staging
      url: ${{ vars.STAGING_URL }}
    env:
      STAGING_DEPLOY_URL: ${{ vars.STAGING_DEPLOY_URL }}
      STAGING_DEPLOY_TOKEN: ${{ secrets.STAGING_DEPLOY_TOKEN }}
      STAGING_URL: ${{ vars.STAGING_URL }}
      SPRING_DATASOURCE_URL: jdbc:mysql://127.0.0.1:3306/shoe_store?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      APP_ADMIN_USERNAME: admin@zephyr.vn
      APP_ADMIN_PASSWORD: TestAdminPassword123!
      APP_JWT_SECRET: ci-staging-jwt-secret-0123456789-abcdefghijklmnopqrstuvwxyz
      SPRING_PROFILES_ACTIVE: test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Build backend
        working-directory: backend
        run: ./mvnw -B verify

      - name: Validate deploy inputs
        run: |
          if [ -z "$STAGING_DEPLOY_URL" ]; then
            echo "STAGING_DEPLOY_URL is required."
            exit 1
          fi
          if [ -z "$STAGING_DEPLOY_TOKEN" ]; then
            echo "STAGING_DEPLOY_TOKEN is required."
            exit 1
          fi
          if [ -z "$STAGING_URL" ]; then
            echo "STAGING_URL is required for post-deploy health checks."
            exit 1
          fi

      - name: Trigger staging deploy
        run: |
          curl --fail-with-body --show-error --silent \
            -X POST "$STAGING_DEPLOY_URL" \
            -H "Authorization: Bearer $STAGING_DEPLOY_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"environment":"staging","source":"github-actions"}'

      - name: Verify staging health
        run: |
          for attempt in $(seq 1 20); do
            if curl -fsS "$STAGING_URL" >/dev/null; then
              echo "Staging health check passed."
              exit 0
            fi
            sleep 10
          done
          echo "Staging health check failed after deploy."
          exit 1

      - name: Rollback note
        if: ${{ failure() }}
        run: |
          echo "Deployment failed. Roll back to the last stable release in your platform and run smoke checks against $STAGING_URL."
